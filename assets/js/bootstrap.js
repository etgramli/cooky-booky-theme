// Tree-shakable Bootstrap entrypoint: Only include components that are actually used by the site.

// Core component for collapse (navbar / collapse elements)
import Collapse from './bootstrap/collapse.js'

export { Collapse }
